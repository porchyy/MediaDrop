# 01: Project Scaffold

**What to build:** ติดตั้ง React + Vite + Tailwind CSS + Lucide React + Google Font (Press Start 2P) ให้ครบ รัน `npm run dev` แล้วเห็นหน้าว่างใน browser ได้

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] package.json มี dependencies ครบ (react, react-dom, lucide-react, tailwindcss, vite, @vitejs/plugin-react)
- [ ] vite.config.js, tailwind.config.js, postcss.config.js ถูกต้อง
- [ ] index.html โหลด Google Font `Press Start 2P` ผ่าน `<link>`
- [ ] `npm run dev` รันได้โดยไม่ error
