# 02: Pixel Design System

**What to build:** CSS foundation สำหรับ pixel art aesthetic — CSS custom properties สำหรับ dark/light palette, scanline overlay, blink animation, pixel-border และ pixel-btn utility classes

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] CSS vars `--bg`, `--surface`, `--accent`, `--border`, `--text`, `--shadow` สำหรับทั้ง light/dark
- [ ] Scanline overlay class ที่ครอบหน้าทั้งหมด
- [ ] `.cursor-blink` animation
- [ ] `.pixel-border` และ `.pixel-btn` utility ใช้งานได้
